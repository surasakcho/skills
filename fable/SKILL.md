---
name: fable
description: Switch the active Claude model to Fable with medium effort. Use when the user types /fable.
---

Edit `~/.claude/settings.json` and set:
- `"model"` to `"fable"`
- `"effortLevel"` to `"medium"`

Preserve all other keys exactly as they are. Then tell the user: "Switched to Fable — effort: medium."
