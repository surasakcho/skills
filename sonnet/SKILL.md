---
name: sonnet
description: Switch the active Claude model to Sonnet with medium effort. Use when the user types /sonnet.
---

Edit `~/.claude/settings.json` and set:
- `"model"` to `"sonnet"`
- `"effortLevel"` to `"medium"`

Preserve all other keys exactly as they are. Then tell the user: "Switched to Sonnet — effort: medium."
